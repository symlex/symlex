<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ code }} {{ error }}">
    <title>{{ code }} {{ error }}</title>
    <link rel="stylesheet" href="/build/error.css">
</head>
<body class="md-theme-default">
<div class="container">
    <!-- Jumbotron -->
    <div class="jumbotron">
        <h1><i class="material-icons">error_outline</i> {{ code }} {{ error }}</h1>
        <p class="lead">{{ message }}</p>
        <a href="javascript:document.location.reload(true);"><button class="md-button md-raised md-accent red">Try This Page Again</button></a>
    </div>
</div>
{% if file %}
    <div class="container">
        <div class="body-content">
            <div class="row">
<pre>
{% if class %}Class: {{ class }}
{% endif %}
File: {{ file }}
Line: {{ line }}
</pre>
                <p>
                    <strong>Stack trace</strong>
                </p>
                <ol>
                    {% for location in trace %}
                        <li><code>
                                {% if location.class %}{{ location.class }}::{% endif %}{% if location.function %}{{ location.function }}(){% endif %}
                                {% if location.file and location.function %} in {% endif %}
                                {% if location.file %}{{ location.file }}{% endif %}
                                {%  if location.line %} line {{ location.line }}{% endif %}
                            </code></li>
                    {% endfor %}
                </ol>
            </div>
        </div>
    </div>
{% endif %}
</body>
</html>